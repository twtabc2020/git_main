# Copilot 指令規範

請在本專案中遵循以下規則：

- 先讀取相關 API 與測試檔案。
- 先確認根因，再修正程式。
- 優先處理最小修正，避免大範圍重構。
- 檢查 API 請求與回應結構是否正確。
- 執行最小範圍驗證，並在回覆時提供驗證方式。
- 保持程式風格一致，不引入無關依賴。
- 若新增功能，補上最小可驗證測試。

本專案的 Python 入口主要位於 `src/python_project/`，測試位於 `tests/`。

優先遵循：
1. 先理解需求
2. 定位問題根因
3. 實作最小修正
4. 驗證結果

切記不要在沒有證據的情況下直接猜測修正方案。
