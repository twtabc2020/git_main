# Python API 專案規範

這份文件是本專案的共用 AI 指引，請所有 AI 助理在處理本專案時遵循以下規則：

## 1. 工作原則
- 先理解需求與 API 入口，再修改程式。
- 先讀相關測試，再決定修正方式。
- 優先檢查輸入參數、headers、payload、response 格式。
- 修正前先確認根因，不要在沒有證據的情況下猜測。
- 優先做最小修正，不做不必要的大範圍重構。
- 修正後以最小驗證方式確認結果。

## 2. 專案目標
- 維持 API 邏輯清楚、穩定、可測試。
- 讓錯誤修正基於事實與測試，而不是推測。
- 保持程式風格一致，避免引入無關依賴。

## 3. 類型與範圍
- 本規範適用於 `src/**/*.py` 與 `tests/**/*.py`。
- 若修改 API 邏輯，請同步確認對應測試是否需要更新。
- 若新增功能，請優先補上最小可驗證測試。

## 4. 回應要求
在回答與修改時，請遵循：
- 說明問題原因
- 說明修正內容
- 列出驗證方式
- 若有風險，提醒可能影響範圍

## 5. 禁止事項
- 不要在沒有證據下直接重寫整段邏輯。
- 不要引入不必要的第三方套件。
- 不要忽略現有測試。
- 不要假設 API 行為，先確認資料流與實際回應。

## 6. 典型處理流程
1. 讀取相關 API 檔案與測試。
2. 確認目前錯誤或需求。
3. 定位 root cause。
4. 實施最小修正。
5. 執行最小驗證。
6. 總結結果與風險。
